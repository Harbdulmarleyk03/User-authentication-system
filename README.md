# User-authentication-system
A scalable authentication system using Django REST Framework with a custom user model (AbstractBaseUser), BaseUserManager, and JSON Web Token (JWT) authentication via SimpleJWT, supporting user registration, login, and logout.
